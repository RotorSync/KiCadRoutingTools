c7babd4 2026-07-28 parity sweep 81a50b9..c7babd4 (4 commits: the placement
coordinate-precision fix, the corpus-prep and writer-level Edge.Cuts strips, and this
lint note). Outcome: ONE PRE-EXISTING GAP CLOSED, ONE NEW GAP OPENED AND RECORDED.
Zero files in kicad_routing_plugin/ this range; every finding is CLI-side.

  * GAP CLOSED -- placement/writer.py wrote footprint positions with `{:.4g}`.
    `%g` counts SIGNIFICANT digits, so any coordinate past 100mm (most of a real
    board) was quantised to 0.1mm and past 1000mm to 1mm: a cap moved to
    x=139.96 was written "140", a 40um error introduced by the writer itself.
    The GUI was ALREADY correct -- fanout_gui moves footprints with
    `mm_to_iu(p['new_x'])`, nanometre-exact -- so this was a genuine CLI/GUI
    DIVERGENCE, not merely a bug: the two fronts placed the same cap in
    different spots. Now `:.6f` (angles `:.4g` -> `:.6g`, which was rounding a
    137.25deg pad to 137.2). Measured on rp2350: moving C20 (x=150.2) by
    (+0.2, -0.35) gave (150.4, 118.3) before and (150.4, 118.35) after.
    Silently coarsened the #130 cap moves / #313 via nudge -- the very
    clearance repair the writer exists to apply.

  * GAP OPENED (recorded, NOT closed) -- kicad_writer.strip_zero_length_edge_cuts
    is wired into output_writer / plane_io / kicad_writer beside the silkscreen
    movers, so every board the CLI WRITES now has its null-length Edge.Cuts
    primitives dropped. The GUI mutates the user's open pcbnew document and never
    calls those writers, so it does NOT. Consequence: same board, two fronts,
    different Edge.Cuts content. The twin would be
    gui_utils.strip_zero_length_edge_cuts_board(board) at the two sites that
    already call move_copper_graphics_to_silkscreen_board (swig_gui apply path,
    planes_gui). NOT shipped blind -- removing footprint-embedded Edge.Cuts items
    via the SWIG object API killed the interpreter in every headless attempt, so
    it needs verifying against a running KiCad. Noted in
    tests/gui_parity/test_cli_postpass_coverage.py, which cannot discover it
    (that gate walks CLI main()s; this lives in the shared writers, same blind
    spot as move_copper_graphics_to_silkscreen).
    WHY IT MATTERS: KiCad grades each null-length primitive as `invalid_outline`
    and then grinds. On pinci (335 of them, 323 inside footprints) kicad-cli DRC
    took 656s; with them dropped, 2.7s -- ~280x -- and every real finding is
    byte-identical (199 phantom invalid_outline vanish; shorting_items 8->8,
    solder_mask_bridge 16->16, unconnected 101->101). --refill-zones is NOT the
    driver (543s with it). 406 corpus boards scanned: median Edge.Cuts segment
    count is 4, so pinci at 5066 is a ~1250x outlier; only 4 boards carry any
    degenerate primitives at all.

  * tests/stress/prep_set2.py (corpus prep) also strips them, from a COPY of the
    source before LoadBoard. Not parity-bearing (no GUI equivalent exists or is
    wanted), but it additionally un-breaks prep's OWN outline rebuild: the
    degenerate segments made GetBoardPolygonOutlines() fail, so prep printed
    "outline left as-is" and silently skipped normalizing. pinci's unrouted
    Edge.Cuts now goes 4731 -> 353 segments, DRC-identical (24 violations either
    way).

  * GATES: test_manifest_plan_parity.py 12936 flag-checks across 334 manifests,
    0 mismatches; param->control 12/12, 0 unresolved. test_cli_postpass_coverage.py
    4 registered, 10 in active CLI use, 1 CLI-only acknowledged, 0 failures.
    wx harnesses were run earlier this session at 7a1dd53 and still apply: the
    writer strip is a provable NO-OP on the rp2350 fixture (0 zero-length
    primitives, 4 Edge.Cuts segments total; the pass returns the content object
    unchanged). test_gui_engine_parity.py VERDICT PARITY, copper sets IDENTICAL
    (1307/1307 segments, 144/144 vias), kicad unconnected 0/0, drc 0/0.
    test_gui_livechain_rp2350.py PASS -- GUI and CLI both grade 0 DRC at every
    stage (create/repair/reconnect/final) at the 0.09 floor.

81a50b9 2026-07-28 parity sweep 4d99b23..81a50b9 (3 commits: the parity-marker
correction, the /Users/andy path purge, and the RUNBOOK <TOOLS_REPO> placeholder +
manifest_to_plan -o alias). Outcome: NO GAPS -- zero routing-engine and zero
kicad_routing_plugin/ files in the range. Headline: ALL THREE wx harnesses were
RUN this session, the first time in seven markers, and all three PASS.

  * NOTHING PARITY-BEARING CHANGED. The range touches no CLI routing script and
    no plugin file. Per-file:
    - manifest_to_plan.py: the ONLY converter change is in main()'s argv parsing
      (accept -o/--output beside the positional, and refuse an output path that
      starts with '-'). Verified confined: the diff hunks are the docstring and
      main() only; FLAG_PARAMS / LIST_FLAGS / BOOL_FLAGS / TOOL_FLAG_* /
      parse_command / plan_steps_from_manifest are UNTOUCHED (0 changed lines),
      so the plan->params mapping gate 1 exercises is bit-identical.
      WHY IT MATTERED: output is positional, and the old raw sys.argv parse took
      the literal string '-o' AS the output path -- `manifest_to_plan.py <m> -o
      plan.json` wrote a file named `-o` and left the real target STALE while
      printing "written to -o" as if it had succeeded. A weeks-old stale plan
      then read as two converter bugs (wrong fanout `kind`, dropped
      `allow_via_in_pad`); NEITHER EXISTED. Fixed so it cannot recur.
    - probe_effective_floors.py (tests/gui_parity): hardcoded repo root ->
      derived from __file__. Diagnostic probe, no gate depends on it.
    - manifest_set*.json (27 files, 391 values): corpus provenance data, not read
      by the converter or the GUI. "file" reduced to basename (every consumer
      only ever used Path(...).name); resolution verified byte-identical.
    - RUNBOOK.md: headless-worker instructions, no CLI/GUI surface.

  * ALL THREE wx HARNESSES RUN AND GREEN (ending the six-marker carry-over; see
    the 4d99b23 entry for why the prerequisites were there all along):
    - test_gui_engine_parity.py: VERDICT PARITY, copper sets IDENTICAL
      (1307/1307 segments, 144/144 vias), kicad unconnected 0/0, drc 0/0. Re-run
      AFTER the 26c51da wiring fixes -- they perturb nothing at default settings,
      as expected since each added value equals the engine default until a user
      changes the control.
    - test_gui_livechain_rp2350.py: PASS -- GUI and CLI both grade 0 DRC at EVERY
      stage (create / repair / reconnect / final) at the 0.09 floor on the
      set11-class rp2350 board.
    - replay_plan_vs_run.py (the strongest harness -- real dialog + real
      PlanExecutor + the real converter, nothing mocked): IDENTICAL on BOTH
      boards tried. set1/megadesk 365/365 segments, 49/49 vias, zones and
      footprints identical, 4/4 steps match. set3/esp_prog 185/185 segments,
      32/32 vias, 4/4 steps match INCLUDING its route_diff step -- which
      exercises the differential_gui call site changed in 26c51da.
      Incidental: esp_prog's RECORDED board carries 16 router DRC / 15 kicad DRC
      while both legs at HEAD grade 0/0 -- engine improvement since that
      recording, not a GUI/CLI gap (the tool reports that drift separately).

  * HARNESS DEFECT FIXED EN ROUTE (4d99b23, recorded here for completeness):
    test_gui_engine_parity reused tests/gui_parity/work/, and both legs route to
    FIXED output names, so route*.py read back the PREVIOUS run's sibling
    .kicad_pro DRC floor. With NO code change: 1st run on a clean dir IDENTICAL,
    2nd and 3rd runs in the same dir reported 3 then 624/644 divergent segments,
    reproducibly. The DEFAULT invocation therefore manufactured a phantom
    divergence on every run after the first -- it briefly looked like this
    session's own fixes had broken plane routing. Workdir is purged at start now;
    consecutive default runs are IDENTICAL. LESSON: A/A with a FRESH workdir
    before bisecting your own change.

GATES RUN GREEN AT HEAD (all four):
  * test_manifest_plan_parity.py: 12286 flag-checks / 316 manifests, 0 mismatches;
    param->control 12/12, 0 unresolved. (Corpus grew mid-session: the sets-21-25
    LLM run is recording new manifests, and ble02_rf_module was re-recorded onto
    qfn_fanout after #500 correctly refused its staggered U2.)
  * test_cli_postpass_coverage.py: 4 registered, 10 in use, 0 failures.
  * test_engine_kwarg_parity.py: 0 CLI-only kwargs, 0 NEW dead controls.
  * test_gui_engine_parity.py: PARITY, copper IDENTICAL.

STILL OPEN (unchanged by this range, carried):
  * The 25 recorded dead diff-tab keys (KNOWN_DEAD_DIFF_KEYS) -- structural: the
    Differential tab's ~30-key get_routing_config vs the ~83-key
    _build_routing_config. Delegating one to the other would make 25 ignored
    Basic-tab controls take effect and CHANGE GUI diff-routing results, so it is
    a PRODUCT DECISION, deliberately not flipped. AWAITING A DECISION.
  * Carried wx items: multi-class net_clearances convergence, a QFN GUI fanout on
    zynq_ad9364 U11, and a GUI diff route on a board with real pairs + Coplanar
    Gap graded with check_impedance.py -- the kwarg gate proves the value REACHES
    the engine; it does not yet prove the resulting geometry hits the CPW target.

--- prior sweeps ---
4d99b23 2026-07-28 parity sweep b9f56e4..937b745 (139 commits: #486 impedance/
coplanar, the GUI/CLI order-canonicalisation wave, #479 plane region joins, #482
trace+movie, #483/#484/#489/#490 audit fixes, #494, #496, #499, #500, #505,
#506/#507, #508 write-list divergence, #509 plane custody, #463, #491).
Outcome: THREE gaps found and fixed (26c51da), a new gate added (4d99b23), and a
false-failure fixed in the wx harness. SUPERSEDES the ab21328 entry, which
recorded "ONE gap found and FIXED (c99ffb4)" -- that was wrong twice over: there
were three, and c99ffb4 ITSELF WAS INERT. Corrected here rather than left
standing.

  * THE INERT FIX (the lesson). c99ffb4 added
    coplanar_gap=config.get('coplanar_gap', 0.0) at the diff engine call. But the
    diff tab does NOT use _build_routing_config (~83 keys, where coplanar_gap
    lives); it has its OWN hand-maintained get_routing_config (~30 keys) inside
    _create_differential_tab. Nothing put 'coplanar_gap' there, so the kwarg
    evaluated to 0.0 forever. Static reading said the fix was correct; driving
    the REAL headless dialog and printing the merged config said ABSENT. VERIFY A
    WIRING FIX BY OBSERVING THE VALUE ARRIVE, never by reading the call site.
  * THREE gaps, all fixed in 26c51da by supplying the keys / forwarding them:
    - coplanar_gap (#486): diff pairs silently routed MICROSTRIP against a CPW
      impedance target (wider trace than CPW-over-ground for the same ohms).
    - add_teardrops (#489 s9): differential_gui has READ config['add_teardrops']
      since #489 with no supplier, so GUI diff routing never applied teardrops.
      planes_gui had the IDENTICAL bug, fixed then with the comment "nothing ever
      supplied it, so the checkbox was dead here" -- the diff tab was missed. The
      dead-shared-control pattern is RECURRENT, which is why it now has a gate.
    - ripup_blocker_select (#424): value present in the diff config but never
      forwarded; absent entirely from the planes shared params. Both CLI mains
      pass it, so a non-default strategy applied to single-ended routing only.
  * NEW GATE tests/gui_parity/test_engine_kwarg_parity.py (4d99b23), covering
    BOTH classes: CLASS 1 (static) a kwarg the CLI passes that the GUI omits;
    CLASS 2 (live) a kwarg the GUI passes that reads a key nothing supplies,
    counted only when a CONTROL of that name exists (no control = the default
    fallback IS the design; 11 plane knobs are deliberately unexposed and are not
    flagged). Includes the explicit #486 coplanar diff-pair case.
    NEGATIVE-TESTED: reintroducing the bug gives exit 1 naming both classes.
  * KNOWN DIVERGENCE RECORDED (not hidden): 25 keys the diff engine call reads
    that the diff tab never supplies (bus_*, *_proximity_*, mps_*,
    crossing_penalty, meander_amplitude, length/time_match_tolerance, ...).
    Structural: the diff tab's ~30-key builder vs the ~83-key
    _build_routing_config; 53 keys exist only in the latter. Those Basic-tab
    controls apply to single-ended routing but are ignored for coupled pairs,
    while route_diff.py's argparse passes every one. The structural fix
    (delegate get_routing_config to _build_routing_config) is a PRODUCT decision
    -- it would make 25 ignored controls take effect and change GUI diff results
    -- so it is baselined, not silently flipped. AWAITING A DECISION.
  * ENGINE SIGNATURE +2 (batch_route 86->88, batch_route_diff_pairs 83->84):
    coplanar_gap (both) and coplanar_nets (route.py only -- route_diff has no
    --coplanar-nets). create_plane 44, generate_bga_fanout 22,
    generate_qfn_fanout 13 all UNCHANGED.
  * create_plane RETURN SHAPE 8 -> 10, GUI already correct (planes_gui unpacks
    10); every early exit routes through _empty_plane_results(return_results),
    which returns 10 for the GUI and 3 for the CLI.
  * Results-data keys the GUI must ACT on, both handled: zone priority
    (SetAssignedPriority + SetPriority fallback) and vias_to_remove.
  * No absolute intra-package imports remain in kicad_routing_plugin/ (the
    #506/#507 silent-apply-abort class). Re-scanned.

GATES RUN GREEN AT HEAD (all four):
  * test_manifest_plan_parity.py: 12166 flag-checks / 314 manifests, 0 mismatches.
  * test_cli_postpass_coverage.py: 4 registered, 10 in use, 0 failures.
  * test_engine_kwarg_parity.py (NEW): 0 CLI-only kwargs, 0 new dead controls.
  * test_gui_engine_parity.py: **RUN THIS TIME** -- VERDICT PARITY, copper sets
    IDENTICAL (1307/1307 segments, 144/144 vias), kicad unconnected 0/0, drc 0/0.

  This ends a SIX-MARKER carry-over. Every prior marker recorded the wx gate as
  "NOT RUN (no wx/pcbnew in this session)". The prerequisites were installed the
  whole time (KiCad python 3.9.13, pcbnew 10.0.0-103, wx 4.2.2a1); the test
  re-execs into KiCad's python itself and builds the dialog headless. Nobody had
  checked. See CLAUDE.md "Parity gates" for the invocation and the probe to run
  BEFORE ever claiming it cannot run here.

  HARNESS BUG FIXED (4d99b23): test_gui_engine_parity reused
  tests/gui_parity/work/ and both legs route to FIXED output names, so route*.py
  read back the previous run's sibling .kicad_pro DRC floor. With NO code change:
  1st run on a clean dir IDENTICAL, 2nd and 3rd runs in the same dir reported 3
  then 624/644 divergent segments, reproducibly. The DEFAULT invocation was
  guaranteed to manufacture a phantom divergence on every run after the first --
  it briefly looked like this sweep's own fixes had broken routing. The workdir
  is now purged at start; consecutive default runs are IDENTICAL.

STILL PENDING a wx session: the multi-class net_clearances convergence check and
a QFN GUI fanout on zynq_ad9364 U11 (carried). NEW: a GUI diff route on a board
with real pairs + Coplanar Gap, graded with check_impedance.py -- the new gate
proves the value REACHES the engine, not yet that the resulting geometry hits
the CPW target.

--- prior sweeps ---
b9f56e4 2026-07-23 parity sweep f445d6b..b9f56e4 (the 0.19.0 release range:
#156 fractional per-layer track_margin / Rust 0.19.0, #465 env-gated impedance
neck-down, #477 copperpour keep-outs in the plane fill model, #478 footprint-
nested zone parsing, and the direction-preference default 5000->250 routability
fix). Outcome: parity CONFIRMED via both no-wx gates + a per-commit code audit;
NO gaps found, NO batch_route*/create_plane/generate_*_fanout kwarg added or
removed (the 77-key param set is UNCHANGED). Release: v0.19.0.

  * #156 track_margin (dab9168, Rust 0.19.0): INTERNAL engine change --
    route_multi/route_with_frontier take a fractional track_margin computed
    inside the shared config (route_reserve_width / _phase_exact_margin /
    track_margins_for_net) from EXISTING params (track_width, per-net widths,
    impedance). No new CLI flag (route*/route_diff argparse unchanged this
    range), no new batch_route kwarg; both fronts inherit. reserve_layer_widths
    is an internal diff-engine stamp policy, not user-facing.
  * #465 impedance neck-down (a6461a4): ENV-GATED (KICAD_IMPEDANCE_NECKDOWN,
    default '1' = allow). No add_argument, no dialog control -- per the env-knob
    convention (inert unless set, default = prior behavior, no front divergence).
  * #477 copperpour keep-outs -> plane fill model (44fd129): extract_keepouts
    records copper_pour_allowed; the pcbnew builder reads keepouts through the
    SAME extract_keepouts(file text) (kicad_parser build_pcb_data_from_board
    ~L3519), so the field flows to the GUI for free (per the commit's own note).
    ZoneFillModel (plane_fill_model, consumed by the shared check_net_
    connectivity that both the CLI grader and swig_gui import) blocks copperpour-
    banned cells on both fronts. No gap.
  * #478 footprint-nested zones (9de56d2): TEXT-parser catch-up -- footprint-
    owned zones (KiCad 7+) were invisible to extract_zones/extract_keepouts,
    but pcbnew loads them NATIVELY, so this brings the text parser UP to the
    pcbnew path's behavior (the CLAUDE.md parser-parity direction that needs no
    mirror). The GUI keepout read (extract_keepouts on the file) also benefits.
  * direction-preference 5000->250 (b9f56e4): value-only default change in
    routing_defaults + the GridRouteConfig dataclass; swig_gui reads the default
    SYMBOLICALLY (control default L832, reset L2270, config fallbacks L2990 /
    differential_gui L1001), so the GUI inherits 250 with no literal 5000
    anywhere. Doc table updated. Fixes the sets-6-11 dp=5000 routability
    regression (+104 incomplete nets, kbic65 98.9%->15.1%).

GATES RUN GREEN AT HEAD:
  * test_manifest_plan_parity.py: 11736 flag-checks across 307 manifests, 0
    mismatches; param->control 12/12, 0 unresolved.
  * test_cli_postpass_coverage.py: 4 registered, 10 in active CLI use, 0
    failures, 0 warnings (bga/qfn run_drc the acknowledged CLI-only report-only
    pass).

NOT RUN THIS PASS: tests/gui_parity/test_gui_engine_parity.py (no wx/pcbnew in
this session) -- carried over. The 77-key batch_route param set is UNCHANGED
this range, so the prior marker's wx VERDICT still covers the engine signature.
Still pending a KiCad-python session (carried from prior markers) PLUS new this
sweep: a GUI net-list/plane check on a copperpour-keepout / footprint-nested-
zone board (e.g. the #217 oracle board) to confirm the pcbnew build_pcb_data
keepout read matches the CLI fill-model split.

--- prior sweeps ---
9466342 2026-07-22 parity sweep 143d097..9473bf8 (the bus-corridor-planning +
dogbone-escape-128 merges and the fill-model / rescue / tap-relocation wave --
93 files, +8067 lines: #128 dog-bone escape, #296-R9 bus corridor planning,
#424 tap relocation / congestion field, #444 SE seam re-ask, #470 via->pad
graze class, #472 direct-first ordering, #473 checkpoint/dead-end hardening,
#475 gr_poly outline, the plane-net manifest sidecar, and the fill-COMPONENT-
aware zone credit). Outcome: parity CONFIRMED via both no-wx gates + a
per-commit code audit, with ONE gap found and fixed (9466342, below). The
recorded SHA (9466342) is the fix commit -- one commit past merge HEAD 9473bf8.

  * GAP FOUND + FIXED (9466342): route_planes' --zone-clearance is `type=float`
    (a NEW flag this range, 0efffb2) but was placed in manifest_to_plan
    BOOL_FLAGS. A recorded `--zone-clearance 0.12` converted to
    `zone_clearance: True`, dropping the value; the plan executor's generic
    param->control loop then set the plane zone-clearance SpinCtrlDouble to
    float(True)=1.0 (range max 2.0, unclamped) and the route_planes block
    checked the override box -- replaying a 0.12 mm pour clearance at 1.0 mm.
    Moved to FLAG_PARAMS. The GUI value path (planes_gui checkbox+spin,
    claude_plan route_planes block, swig_gui reset) was already correct; only
    the converter classification was wrong. The manifest_plan_parity gate
    structurally could not catch this -- no corpus manifest yet uses the new
    flag, and its 12-param control-resolution set doesn't include it.

  * ONE NEW batch_route KWARG -> 76-key set becomes 77:
    - ripup_blocker_select (--ripup-blocker-select, #424 audit; routing_config
      field default 'count'). Threaded through ALL FOUR route mains
      (route/route_diff/route_planes/route_disconnected_planes) AND fully GUI-
      wired: swig_gui wx.Choice (defaults.RIPUP_BLOCKER_SELECT_CHOICES) + config
      dict + reset_params_to_defaults + the batch_route call site;
      settings_persistence save/restore; manifest_to_plan FLAG_PARAMS
      (--ripup-blocker-select); test_gui_engine_parity config map. Default
      'count' matches on both fronts (byte-identical when unset).
  * NEW GUI-VISIBLE CHOICES / CONTROLS, all wired:
    - --ordering gains a `bus` choice (ec8e21a) -> ordering_strategy: swig_gui
      dropdown adds "bus"; manifest_to_plan maps --ordering -> ordering_strategy
      (121fb40, was carried as the no-match 'ordering'); wx test now step-drives
      ordering_strategy.
    - --escape-method gains `dogbone` (#128, 27b35b4): fanout_gui BGA panel
      ESCAPE_METHODS tuple + dropdown entry + tooltip; claude_plan _apply_special
      reads opts.ESCAPE_METHODS dynamically so 'dogbone' round-trips; converter
      carries it via the generic unknown-flag fallthrough (verified 'dogbone').
    - --zone-clearance override: planes_gui CreatePlanesOptionsPanel gets a
      checkbox+spin row (unchecked = follow routed clearance auto-stepping to
      the fab floor; checked = typed value); get_config returns None when
      unchecked; PlanesTab create_plane / repair passes config.get('zone_
      clearance') (no default -> None); swig_gui reset unchecks + disables;
      claude_plan checks the box when a plan value is present.
    - --vertical-attraction-radius (new route.py argparse exposure of the
      existing net-agnostic cross-layer stacking radius): existing swig_gui
      control + generic fallthrough (verified 0.6 round-trips); tooltips
      reworded on both fronts to the net-agnostic meaning.
  * SHARED-FUNCTION features -> both fronts inherit, no per-front wiring:
    - Plane-net manifest sidecar (075d1bf/d1cca2f/8bc7ba7): CLI mains write via
      plane_io.record_plane_manifest / carry_plane_manifest and pre-filter net
      selection via apply_plane_manifest_exclusions (a PRE-engine selection
      filter, correctly NOT a post-pass). GUI mirror: claude_plan._plan_plane_
      nets (whole-plan plane steps UNION the on-disk manifest -- covers steps
      that run before the plane step, the CLI's fanout-before-planes hole) +
      _drop_plane_nets on route/fanout selection; planes_gui writes the manifest
      after a plane run. Both fronts wired.
    - Fill-COMPONENT-aware zone credit (607f856/8779b70/d5b2aea/f9c703c/
      9473bf8): check_net_connectivity (defined in check_connected.py, imported
      by BOTH the CLI grader and swig_gui) gained a pcb_data param + the
      plane_fill_model component-labeling credit. GUI-parity fix: swig_gui's
      net-list "hide connected" now passes pcb_data=self.pcb_data (without it a
      pinched pour island graded connected here while KiCad DRC showed it open).
    - #444 SE seam re-ask, #470 via->pad graze close_soft_joints class, #424 tap
      relocation / congestion field / fragility field, #472 direct-first
      ordering, #473 dead-end/checkpoint protection, rip_restore, stub layer
      switching, bus_corridor/bus_detection, congestion_field: all inside the
      shared engine (single_ended_*, phase3_routing, diff_pair_routing,
      batch_route path) -> both fronts inherit. Rust 0.18.4/0.18.5 (cross-layer
      bus attraction, P2/P3 precompute) below the Python API, front-agnostic.
    - #475 gr_poly free-form outline crash fix + zone_clearance=None repair
      (741bfe8): parser/engine + planes path; the GUI crash fix is in the shared
      planes flow.
  * ENV-GATED DEBUG KNOBS (inert unless set, default = prior behavior, no
    dialog control by convention): KICAD_BUS_STRICT/OFFLANE_MULT/RIP_RESISTANCE,
    KICAD_DIRECT_FIRST (default '1', inside the shared engine ordering),
    KICAD_FANOUT_DIRECT / _TOWARD_TARGETS, KICAD_ISLAND_LAUNCH,
    KICAD_UNDERPAD_OUTER_RINGS, KICAD_STOP_AFTER/_FILE/_CLEANUP (checkpoint
    abort; only conditionally SKIPS the shared run_post_route_cleanup, off by
    default so both fronts run cleanup identically), KICAD_NET_STORY. None
    change a default that diverges between fronts.
  * CLI-only, no GUI surface (grading/corpus/debug): grade_final honest-DRC,
    kicad_unconnected authoritative count, net_forensics/net_story debug dumps,
    stress harness (run_board/run_queue/board_layer_images, set3monster corpus).

GATES RUN GREEN AT HEAD (post-fix):
  * test_manifest_plan_parity.py: 11736 flag-checks across 307 manifests, 0
    mismatches; param->control resolution 12/12, 0 unresolved.
  * test_cli_postpass_coverage.py: 4 registered, 10 in active CLI use, 0
    failures, 0 warnings (bga/qfn run_drc the one acknowledged CLI-only
    report-only pass).

NOT RUN THIS PASS: tests/gui_parity/test_gui_engine_parity.py (no wx/pcbnew in
this session) -- carried over. NOTE the batch_route param set GREW this range
(76 -> 77: +ripup_blocker_select), so the prior marker's wx VERDICT no longer
covers the exact signature; the config map IS updated and both fronts pass the
new kwarg with matching 'count' default, but a KiCad-python session should
re-run the wx engine-parity test to reconfirm. Still pending a KiCad-python
session (carried): multi-class net_clearances convergence, a GUI-engine diff
route, a QFN GUI fanout on zynq_ad9364 U11 (#446 tips on-grid), a slotted-board
(sofle) GUI plane vs the CLI copperpour keepout sexprs -- and NEW this sweep:
(1) a GUI route_planes with an explicit --zone-clearance to confirm the fixed
converter value reaches create_plane; (2) a dog-bone (#128) GUI BGA fanout;
(3) a GUI net-list check on a fill-COMPONENT board to confirm the pcb_data zone
credit matches check_connected.

--- prior sweeps ---
143d097 2026-07-20 parity sweep a92ccec..143d097 (the #446 QFN fan-tip
re-snap, #452 Rust diagonal direction cost / rust_router 0.18.3, the corpus
expansion to sets 11-25, and a self-reverted bga_fanout experiment). Outcome:
parity CONFIRMED via both no-wx gates + a per-commit code audit. NO
batch_route*/create_plane/generate_*_fanout kwarg was added or removed, so the
76-key param set is IDENTICAL to the a92ccec marker. Release: v0.18.3.

  * SHARED engine functions -> both fronts inherit, no wiring:
    - #446 QFN fan-tip re-snap (c0737e9): _snap_tip_on_grid is a PRIVATE helper
      called from inside generate_qfn_fanout's #123 foreign-pad clearance pass.
      fanout_gui._run_qfn_fanout imports and calls generate_qfn_fanout directly
      (fanout_gui.py ~L1366), so the GUI inherits it. No new parameter: grid_step
      and the `grazes` gate are both already in scope at the call site, and the
      generate_qfn_fanout signature is untouched (the def line appears only as a
      diff hunk header). Nothing to add to claude_plan / manifest_to_plan /
      reset_params_to_defaults.
    - #452 diagonal direction cost (a0e7fd4, rust_router 0.18.3): router.rs
      charges the wrong-axis component of DIAGONAL moves. Below the Python API
      and therefore front-agnostic -- both fronts load the same grid_router.
  * NO-OP (self-reverted, nothing survives at HEAD):
    - 16d3822 bga_fanout snap_exit_end candidate ladder, reverted by 143d097.
      Net diff over the pair is empty; no audit surface.
  * NO GUI SURFACE (corpus/test data only):
    - c82a7ff corpus expansion to sets 11-25 (+223 boards): tests/stress
      manifests + fetch/prep scripts only. Note this GREW the manifest_plan
      parity corpus -- the converter gate now sweeps 158 manifests / 6229
      flag-checks (was 6201), all still 0 mismatches.

GATES RUN GREEN AT HEAD:
  * test_manifest_plan_parity.py: 6229 flag-checks across 158 manifests,
    0 mismatches; param->control resolution 12/12, 0 unresolved.
  * test_cli_postpass_coverage.py: 4 registered, 10 in active CLI use,
    0 failures, 0 warnings (bga/qfn run_drc remains the one acknowledged
    CLI-only report-only pass).

NOT RUN THIS PASS: tests/gui_parity/test_gui_engine_parity.py (no wx/pcbnew in
this session) -- carried over. With the 76-key param set unchanged the prior wx
VERDICT PARITY still covers the engine signature. Still pending a KiCad-python
session (carried): multi-class net_clearances convergence, a GUI-engine diff
route, a QFN GUI fanout -- and, carried from the a92ccec marker, creating a
plane via the GUI on a slotted board (sofle) to confirm the pcbnew rule areas
match the CLI's copperpour keepout sexprs. NEW for next wx session: a GUI QFN
fanout on zynq_ad9364 U11 to confirm the #446 tips land on-grid there too.

--- prior sweeps ---
a92ccec 2026-07-20 parity sweep ada9778..a92ccec (the #448/#449/#450/#451/#445
burndown: NPTH-slot-as-edge family, meander skip-offset, custom-pad anchor
tessellation, fanout rect-pad stamps, cap-mover via gate, plus the grading/
compare work). Outcome: parity CONFIRMED via both no-wx gates + a per-commit
code audit. NO batch_route*/create_plane kwarg was added or removed (route.py /
route_diff.py untouched in the whole range), so the 76-key param set is
IDENTICAL to the ada9778 marker. Every behavioral change is one of:

  * SHARED engine functions -> both fronts inherit, no wiring:
    - #449 meander skip-offset (c71d5a8): generate_trombone_meander /
      generate_centerline_meander internals (batch_route length matching).
    - #448 slot obstacle band (1ddf220): add_drill_hole_obstacles grows slot
      keep-outs to the edge clearance (shared obstacle map).
    - #448 in-pad via edge floor (7492ae9): _place_shrunk_via_in_pad
      (single_ended_routing, shared last-resort rescue).
    - #451 fanout rect-pad stamps (b3e7ace): bga_fanout/underpad occupancy --
      fanout_gui calls generate_bga_fanout, inherits.
    - #445 cap-mover via gate (a92ccec): placement/fanout_clearance
      _via_shortfalls + hard/cost gates -- fanout_gui calls
      repair_fanout_clearance, inherits.
    - #447 stamped-width edge band (0bb71e4): add_board_edge_obstacles callers
      audited -- all engine-side (obstacle_map x2, plane_region_connector).
    - #441 plane edge pin (ab2d523): effective_board_edge_clearance resolver
      inside repair_planes (engine); registered DISCOVERY_EXEMPT (16e6407).
  * NEW GUI WIRING (done with the engine change, this sweep's one addition):
    - #448 NPTH-slot copperpour keepouts (1ddf220): create_plane computes them
      BEFORE the dry_run/write split; CLI writes generate_keepout_zone_sexpr,
      planes_gui applies pcbnew RULE AREAS ({'keepout': True} entries in the
      EXISTING new_zones list -- no return-shape change, no new param, so
      claude_plan / manifest_to_plan / reset need nothing; dedupe by zone name
      both fronts).
  * PARSER PARITY (the CLAUDE.md text-vs-pcbnew rule), checked explicitly:
    - #450 custom-pad anchor tessellation (35a7f88) is a TEXT-parser fix; the
      pcbnew path already uses pad.GetEffectivePolygon() (KiCad-exact, finer
      than the new adaptive 5um polygon) -- parity holds with NO mirror needed.
    - Copper-GRAPHIC circles remain a 16-gon in BOTH paths (kicad_parser
      gr_circle + the pcbnew _CIRC branch) -- equally coarse on both fronts,
      noted as a shared-precision observation, not a front divergence.
  * GRADING/COMPARE TOOLS only, NO GUI surface: check_drc (2ce4fcb slot edge +
    accepted publications, 3839c6e capsule helper + derive-once margin +
    worst-overlap bbox checks, 35a7f88 pad-via pair dedupe),
    kicad_drc_compare (respect_edge_severity, copper-anchored edge items,
    all-pad hole_clearance design-condition drop), ab_replay_grade (4abb1b3),
    fix_outline_gaps.py (corpus board-prep, b13675c).

GATES RUN GREEN AT HEAD:
  * test_manifest_plan_parity.py: param->control 12/12, 0 unresolved.
  * test_cli_postpass_coverage.py: 0 failures (bga/qfn run_drc the one
    acknowledged CLI-only report-only pass; effective_board_edge_clearance
    registered DISCOVERY_EXEMPT).

NOT RUN THIS PASS: tests/gui_parity/test_gui_engine_parity.py (no wx/pcbnew in
this session) -- carried over; with the 76-key param set unchanged the prior wx
VERDICT PARITY still covers the engine signature. Still pending a KiCad-python
session (carried): multi-class net_clearances convergence, a GUI-engine diff
route, a QFN GUI fanout -- plus NEW for next wx session: create one plane via
the GUI on a slotted board (sofle) and confirm the pcbnew rule areas match the
CLI's copperpour keepout sexprs.

--- prior sweeps ---
ada9778 2026-07-19 parity sweep ad1bd5e..ada9778 (the #441 residual-DRC wave:
h2h / hole_clearance / shorting_items / edge fab-floor / diff-pair gap floor /
cp-drop-floor prevention / picodvi detour weld / ghoul NPTH nudge, plus the #442
Class-B grading fix). Outcome: parity CONFIRMED via both no-wx gates + a
per-commit code audit. No new CLI-visible engine param requires GUI wiring -- the
76-key batch_route* signature set is UNCHANGED (no engine kwarg added/removed),
and every behavioral change is one of:

  * SHARED engine/cleanup/obstacle functions -> both fronts inherit, no wiring:
    - h2h (ade0fe9/83afbac): drill hole-to-hole disc added to obstacle_cache
      precompute_net_obstacles + obstacle_map add/remove-vias stampers.
    - hole_clearance (b74c3e3/ef3e0f9): ring-uncovered plated pad treated as
      NPTH in obstacle_map.add_drill_hole_obstacles (+ check_drc, grading-side).
    - shorting_items (4b955ce/f60273d): per-net accept gate in
      placement/fanout_clearance.repair_fanout_clearance.
    - edge sub-class C (964ccce): per-layer pad-own-copper landing exemption in
      obstacle_map (replaced the removed net-agnostic reach exemption; JSON
      intentional_edge_band emission removed from the route mains + grader).
    - picodvi detour weld (52f9661): weld_redundant_grazing_detours wired INTO
      the shared run_post_route_cleanup graze stage -- both fronts' route calls
      run the pipeline, so the GUI inherits it.
    - ghoul NPTH nudge (54ed114): via-copper-vs-NPTH-hole term added inside the
      shared nudge_grazing_vias.
    - edge fab-floor + explicit override (b1e5ced/32d9434):
      fix_kicad_drc_settings.effective_board_edge_clearance / fab_edge_floor --
      the ENGINE reads these; both route mains AND the GUI writeback call them.
  * ALREADY GUI-WIRED this session:
    - diff-pair gap floor (ffea47e): engine batch_route_diff_pairs floors
      diff_pair_gap up to clearance FIRST (GUI routing inherits, since the GUI
      calls that engine fn); differential_gui writeback ALSO floors the recorded
      gap = max(cfg gap, cfg clearance) (~L1281). It's an automatic floor on the
      EXISTING diff_pair_gap param (no new control), so claude_plan /
      manifest_to_plan / reset need no change.
    - GUI edge re-clamp removal (3b9fdff): swig_gui _effective_board_edge_
      clearance dropped its redundant route-time board-min re-clamp so the edge
      param matches the other five basic-tab floors (the shared Obey-DRC
      interactive validation still enforces the board minimum).
  * GRADING-TOOL only, NO GUI surface: check_drc.py (b1e5ced edge floor,
    b74c3e3 ring-uncovered hole, ada9778 #442 same-net track->own-hole exemption).
  * CLI-only, no GUI counterpart (the GUI operates on the LIVE board and never
    cp's a file): copy_board.py (new) + fix_kicad_drc_settings.warn_if_missing_
    project_floor wired into the 4 route mains (65078be) -- registered
    DISCOVERY_EXEMPT in test_cli_postpass_coverage.

GATES RUN GREEN AT HEAD:
  * test_manifest_plan_parity.py: param->control 12/12, 0 unresolved.
  * test_cli_postpass_coverage.py: 0 failures (bga/qfn run_drc the one
    acknowledged CLI-only report-only pass; weld_redundant_grazing_detours +
    warn_if_missing_project_floor both registered).

NOT RUN THIS PASS: tests/gui_parity/test_gui_engine_parity.py (no wx/pcbnew in
this session) -- carried over, as in the prior two markers. No batch_route*
kwarg changed, so the 76-key param set is IDENTICAL to the ad1bd5e marker and
that marker's wx VERDICT PARITY still covers the engine signature. Still pending
a KiCad-python session (carried from prior markers): multi-class net_clearances
convergence, a GUI-engine diff route, a QFN GUI fanout.

--- prior sweeps ---
ad1bd5e 2026-07-19 parity sweep 810ce7d..ad1bd5e (the #436/#438 cross-class
cleanup umbrella, #426/#427/#429 agent fixes, the replay clobber guard, and
#435 per-pair diff geometry + single-ended per-net track width). Outcome:
parity CONFIRMED -- wx test_gui_engine_parity VERDICT PARITY (kicad unconnected
0/0, drc 0/0) + both no-wx gates pass. New CLI-visible engine params and their
GUI wiring:
  * #436/#438 umbrella (cross-class + board-edge aware cleanup): entirely inside
    the shared cleanup_pipeline, which reads config.net_clearances /
    config.board_edge_clearance -- both fronts inherit, no per-front wiring.
  * #435 diff geometry: diff_pair_{width,gap}_from_class threaded through
    route_diff main (argparse absence) AND differential_gui (override checkbox);
    claude_plan _GEOMETRY_OVERRIDE_CHECKS extended with diff_pair_width/gap ->
    their diff-tab boxes; manifest_to_plan already maps --diff-pair-gap and
    route_diff --track-width; reset already resets the boxes.
  * #435 single-ended companion: track_width_from_class threaded through route.py
    main (argparse absence) AND swig_gui (track_width_check unchecked + no
    impedance); track_width_check already in claude_plan's override map + reset.
  * #427 edge-severity + clobber guard: check_drc.py / tests-stress only (no GUI
    surface). No wiring gaps found.

--- prior sweeps ---
810ce7d 2026-07-17 parity sweep 6e88568..810ce7d (the #217/#412 connectivity
wave, ~37 commits: Tier-1 levers, T5 custody + SE member fallback, net-tie
support, rescue-window fence, full-stack layer normalization, #416 terminal
connector, #418 pad stroke extent, #419 mimalloc purge, #420 oracle fast
project, #422 static bitmap / Rust 0.18.2, #265 per-cluster swaps, PR415
connection_width grading merge). Outcome: parity CONFIRMED via the no-wx
gates + placement audit; no wiring gaps found.

AUDIT: no new CLI flags survive at HEAD. The one flag added during the wave
(--failed-first-restart, with full CLI/GUI/manifest/parity threading) was
REVERTED after measuring zero real-board wins — its threading went with it.
Every behavioral change of the wave lives inside shared engine functions
(batch_route, batch_route_diff_pairs, create_plane, the
route_disconnected_planes repair path, close_soft_joints,
apply_single_ended_layer_swaps, plane_pad_tap/try_tap_pad), so both fronts
inherit them with no per-front wiring:
  * PlaneComponentOracle: internal to the shared repair path (plane_oracle
    param defaults None; oracle-less callers byte-identical).
  * T5 casualty reconcile + coverage gate + honest sweep: inside batch_route;
    additive results keys only.
  * Diff pair_reports / casualty_reconcile / single_ended_fallback: inside
    batch_route_diff_pairs; keys mirrored into GUI results_data by the same
    engine code.
  * Full-stack layer normalization: inside the three shared engine functions
    (route_disconnected_planes exempt by design, documented inline).
  * #416 connector + narrow_pad_joint: close_soft_joints + check_weird
    (shared cleanup/checker).
  * #265 per-cluster assignment: PHASE 3 of apply_single_ended_layer_swaps.
  * rust_alloc.py (MIMALLOC_PURGE_DELAY=0): wired at every CLI entry point
    AND both GUI call sites (per #419 agent; spot-verified imports).
  * Rust 0.18.2 static bitmap: below the Python API, front-agnostic;
    neutrality proven bit-identical (total_iterations).

GATES RUN GREEN AT HEAD:
  * test_manifest_plan_parity.py: 6201 flag-checks, 0 mismatches;
    param->control resolution 12/12.
  * test_cli_postpass_coverage.py: 0 failures (bga/qfn run_drc remains the
    one acknowledged CLI-only report-only pass).

NOT RUN THIS PASS: tests/gui_parity/test_gui_engine_parity.py (needs KiCad
python/wx). No batch_route kwarg was added or removed at final HEAD, so the
76-key param set is unchanged from the prior marker. Carried over from the
prior marker and still pending a KiCad-python session: the multi-class
net_clearances convergence check, a GUI-engine diff route, and a QFN GUI
fanout. Next audit picks up from this marker.
