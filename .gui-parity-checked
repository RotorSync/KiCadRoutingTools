HEAD 2026-07-30 parity audit 4db8c18..81d1dda (4 commits). Outcome: no gaps.

  * 9d34f30 + 13031ae: the previous audit's own fix/marker commits.
  * 051f456 (#539) and 526ca3e (#543) are engine-side fixes inside shared
    functions (guaranteed-join gate's batch_route h2h forwarding; incremental
    via-map stamps mirroring the builders' cross-class/#498 pricing) — both
    fronts inherit them; no new flags, params, or post-passes.
  * 81d1dda adds route.py --force-reroute (#515/PR#533), wired end to end in
    the same commit: engine kwarg force_reroute in batch_route (strip before
    the already-connected filter, #521-gated, restore-on-no-copper custody),
    same-named Basic-tab checkbox (config build + call site + reset_params_
    to_defaults + settings_persistence save/load; plan executor applies it by
    generic name match), and manifest_to_plan BOOL_FLAGS entry (verified by
    converting a synthetic manifest carrying the flag). Second new kwarg
    net_name_patterns is CLI-only BY DESIGN (raw pre-expansion --nets
    patterns as the #521 protection-override source; the GUI's per-net
    checkboxes are exact selection, so it correctly passes None) and is
    popped from _reconcile_kwargs/KWARGS dump, so it is not a parity key.
  * Gates at HEAD (2026-07-30): test_manifest_plan_parity 14767 checks 0
    mismatches; test_cli_postpass_coverage 0 failures; test_gui_engine_parity
    VERDICT: PARITY — segments CLI=GUI=1307, vias 144=144, DRC 0/0,
    kicad_unconnected 0/0, copper sets IDENTICAL (run twice: before and
    after the net_name_patterns change). Functional: tests/
    test_force_reroute.py 6-scenario CLI end-to-end passes.
